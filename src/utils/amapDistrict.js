/**
 * Get district boundary polygon. Stub: no AMap; returns empty array.
 * @param {string} district
 * @param {string} [level]
 * @returns {Promise<Array<{lng: number, lat: number}[]>>}
 */
export function getDistrictBoundary(district, level = 'district') {
  return Promise.resolve([]);
}

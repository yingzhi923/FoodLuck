/**
 * Reverse geocode: get address string from (lng, lat).
 * Stub: no external geocoding service; returns empty string.
 * @param {number} lng
 * @param {number} lat
 * @returns {Promise<string>}
 */
export function getAddressFromCoords(lng, lat) {
  return Promise.resolve('');
}
